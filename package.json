{
  "name": "juwa",
  "private": true,
  "version": "0.1.0",
  "description": "Juwa \u2014 casino-style gaming platform",
  "workspaces": [
    "packages/*",
    "app"
  ],
  "scripts": {
    "build": "npm run build --workspace @juwa/money && npm run build --workspace @juwa/economy && npm run build --workspace @juwa/ui && npm run build --workspace @juwa/engine && npm run build --workspace @juwa/server && npm run build --workspace @juwa/api",
    "test": "npm run build && node --test \"packages/*/dist/**/*.test.js\"",
    "typecheck": "npm run build && tsc -p app/tsconfig.json",
    "rtp": "npm run build && node packages/engine/dist/tools/rtp.js",
    "verify": "npm run test && npm run typecheck",
    "test:db": "npm run build && JUWA_TEST_DATABASE_URL=${JUWA_TEST_DATABASE_URL:-postgres://postgres@localhost:5432/juwa_play} node --test \"packages/server/dist/**/*.test.js\"",
    "migration": "node scripts/build-migration.mjs",
    "check": "node scripts/check-deployment.mjs",
    "build:web": "npm run build --workspace @juwa/money && npm run build --workspace @juwa/economy && npm run build --workspace @juwa/ui && npm run build:web --workspace @juwa/app",
    "rtp:catalogue": "npm run build --workspace @juwa/money && npm run build --workspace @juwa/engine && node packages/engine/dist/tools/catalogue-rtp.js"
  },
  "engines": {
    "node": ">=20"
  }
}
